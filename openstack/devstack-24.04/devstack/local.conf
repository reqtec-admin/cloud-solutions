[[local|localrc]]
ADMIN_PASSWORD=secret
DATABASE_PASSWORD=$ADMIN_PASSWORD
RABBIT_PASSWORD=$ADMIN_PASSWORD
SERVICE_PASSWORD=$ADMIN_PASSWORD
HOST_IP=10.123.123.124

# Ubuntu 24.04 compatibility settings
# Increase service timeout to handle slower startup on Ubuntu 24.04
SERVICE_TIMEOUT=300

# Python 3.12 compatibility (Ubuntu 24.04 default)
# Ensure Python 3 is used
USE_PYTHON3=True

