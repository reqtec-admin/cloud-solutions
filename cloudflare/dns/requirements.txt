# Core runtime (equivalent to old ansible 2.17.x)
ansible-core==2.18.1

# Core dependencies (these are always needed; pin if you want ultra-reproducibility)
jinja2>=3.1.0
PyYAML>=6.0
cryptography>=3.3